import * as React from 'react';
import Navigation from './Navigation';

function Header () {
  return (
    <header>
      <h1>Header</h1>
      <Navigation />
    </header>
  );
}

export default Header;
